name: Linting and testings

on:
  push:
    branches:
      # On all commits except from main branch
      - '**'
      - '!main'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Linting
        uses: Projects-for-Fun/go-github-actions/actions/linting@v0.0.2

  unit-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run unit tests
        uses: ./actions/unitTests
        with:
          SAVE_REPORTS: false

  integration-tests:
    name: Integration tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run integration tests
        uses: ./actions/integrationTests
        with:
          SAVE_REPORTS: false
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

